<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lec.spring.repository.ChatRepository">
        <select id="findRoomById" resultType="com.lec.spring.repository.ChatRepository">
            SELECT
            room_id,
            post_id,
            createDate,
            lastUpdateDate,
            subject,
            roomState,
            FROM
            s1_chatroom
            WHERE
            room_id = #{room_id}

    </select>

</mapper>